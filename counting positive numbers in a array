#include<stdio.h>

main()
{
	int count = 0;
	int arr[5];
	
	for(int i = 1;i<=5;i++)
	{
		printf("Enter value\n");
		scanf("%d",&arr[i]);
		if (arr[i] > 0)
		{
			count++;
		}
	}
printf("%d",count);
}
